// Bridges the MSG91 widget to the backend's POST /auth/verify-phone.
// Re-exports the feature hook for backwards compatibility.

export { useVerifyPhone } from "@/features/auth/hooks/useVerifyPhone";
