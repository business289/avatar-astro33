# Backend API base URL for local dev (`npm run dev`).
# Vite only loads this file in development mode — .env.production (used by
# `vite build` / Vercel) is a separate file and never applies here.
VITE_API_URL=http://localhost:5000/api

