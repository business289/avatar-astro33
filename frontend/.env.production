# Backend API base URL used for production builds (Vercel).
# Vite loads this automatically during `vite build` — no dashboard config needed.
VITE_API_URL=https://avatar-astro33-backend.onrender.com/api
